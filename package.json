{
  "name": "bioledger-blockchain",
  "version": "1.0.0",
  "description": "BioLedger – Blockchain infrastructure for biometric AI lifecycle anchoring",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:local": "hardhat run scripts/deploy.js --network localhost",
    "deploy:polygon": "hardhat run scripts/deploy.js --network polygonAmoy",
    "node": "hardhat node",
    "clean": "hardhat clean"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0",
    "axios": "^1.6.0",
    "dotenv": "^16.0.0",
    "ethers": "^6.9.0",
    "form-data": "^4.0.0",
    "merkletreejs": "^0.3.11",
    "msgpack-lite": "^0.1.26"
  }
}